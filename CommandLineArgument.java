package com.ravi;

public class CommandLineArgument {
    public static void main(String[] args)
    {
        System.out.println(args[0]);
        // in command line argument we gives input in the cmd before running the code.
        // in command line argumennt user pass the directly input the value to the main class
        // as an argument in the String.
    }
}
